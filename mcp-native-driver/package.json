{
  "name": "mcp-native-driver",
  "version": "1.0.0",
  "description": "Local MCP server that drives the real Tauri desktop app (native WebKitGTK webview, real Rust IPC, real database) via tauri-driver/WebDriver — a second testing surface alongside Playwright's browser-mock preview.",
  "type": "module",
  "private": true,
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "@types/node": "^20.14.0"
  },
  "engines": {
    "node": ">=20"
  }
}
