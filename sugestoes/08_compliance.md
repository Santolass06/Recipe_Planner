# Compliance — 9.25 → 10

1. Fix `get_recent_activity` — adicionar `WHERE event_id IS NULL` (db.rs:3641) — 5min
2. Actualizar contagens de linhas no PROJECT.md (god-components) — 10min
3. Arquivar HANDOFF_AGENT.md ou actualizar com estado real — 15min
4. Adicionar badge no README com estado do CI quando existir
