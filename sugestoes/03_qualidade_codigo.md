# Qualidade Código — 5.5 → 10

1. Extrair `Unit::from_str()` + `Display` — resolve duplicação — 3h
2. Substituir `unwrap()` por `expect("contexto")` ou `?` — 2h
3. Extrair funções >100 linhas em db.rs — 4h
4. Remover `any` abuse em RecipesPage.tsx — tipos concretos — 1h
5. Substituir `chrono::Duration::days()` deprecado — 30min
6. Remover dead code (ShoppingListWithItems struct, funções órfãs) — 1h
