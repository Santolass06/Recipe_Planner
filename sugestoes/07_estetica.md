# Qualidade Estética — 7 → 10

1. Adicionar CSS que falta (`.data-table`, `.modal-body`, `var(--color-border)`) — 1h
2. `:focus-visible` em todos os botões e links — 30min
3. Media queries para responsividade — 2h
4. Substituir inline styles (~60%) por classes CSS — 4h
5. `aria-labelledby` nos modais + focus trap — 1h
6. Skeleton loaders em vez de spinner genérico — 2h
7. Estado de erro/validação inline nos formulários — 2h
